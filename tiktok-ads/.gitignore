# Secrets — never commit
.env.local
.env

# Generated video. Large binaries in a git-backed KB bloat history fast.
# Keep the .prompt.txt files: they are the reviewable record of each generation.
campaigns/**/*.mp4

# Written by `generate-clip.mjs --dry-run`; regenerate on demand
fixtures/last-request.json

# Node
node_modules/
